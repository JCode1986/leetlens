{
  "id": 91,
  "slug": "decode-ways",
  "title": "Decode Ways",
  "difficulty": "Medium",
  "categories": [
    "Strings",
    "Dynamic Programming"
  ],
  "patterns": [
    "1D Dynamic Programming"
  ],
  "aliases": [
    "decode ways",
    "number of decodings",
    "decode string"
  ],
  "keywords": [
    "dynamic programming",
    "string",
    "decode",
    "digits"
  ],
  "summary": "Solve Decode Ways using the standard optimized interview approach.",
  "hint": "Look for the 1D Dynamic Programming structure that reduces repeated work.",
  "quickAnswer": {
    "pattern": "1D Dynamic Programming",
    "idea": [
      "Decode count depends on one-digit and two-digit choices.",
      "A zero only works as part of 10 or 20.",
      "Keep rolling counts for previous positions."
    ],
    "complexity": {
      "time": "O(n)",
      "space": "O(1)"
    }
  },
  "approach": [
    "Identify the state or invariant needed for Decode Ways.",
    "Use 1D Dynamic Programming to process the input efficiently.",
    "Update the result while preserving the invariant.",
    "Return the final value after all relevant input is processed."
  ],
  "pseudocode": [
    "if string starts with zero:",
    "    return 0",
    "",
    "prevTwo = 1",
    "prevOne = 1",
    "",
    "for each position from 1:",
    "    current = 0",
    "    if one digit is valid:",
    "        current += prevOne",
    "    if two digits are valid:",
    "        current += prevTwo",
    "    move rolling counts forward",
    "",
    "return prevOne"
  ],
  "complexity": {
    "time": "O(n)",
    "space": "O(1)"
  },
  "edgeCases": [
    "Empty or smallest valid input.",
    "Duplicate or repeated values.",
    "Result is not found.",
    "Large input near expected limits."
  ],
  "solutions": {
    "javascript": {
      "desktop": [
        "function numDecodings(s) {",
        "  let prev2 = 1, prev1 = s[0] === '0' ? 0 : 1;",
        "  for (let i = 1; i < s.length; i += 1) { let cur = s[i] === '0' ? 0 : prev1; const two = Number(s.slice(i - 1, i + 1)); if (two >= 10 && two <= 26) cur += prev2; prev2 = prev1; prev1 = cur; }",
        "  return prev1;",
        "}"
      ],
      "g2": [
        "function numDecodings(s) {",
        "  let prev2 = 1, prev1 = s[0] === '0' ? 0 : 1;",
        "  for (let i = 1; i < s.length; i += 1) {",
        "    let cur = s[i] === '0' ? 0 : prev1;",
        "    const two = Number(s.slice(i - 1, i + 1));",
        "    if (two >= 10 && two <= 26) {",
        "      cur += prev2;",
        "    }",
        "    prev2 = prev1;",
        "    prev1 = cur;",
        "  }",
        "  return prev1;",
        "}"
      ]
    },
    "python": {
      "desktop": [
        "class Solution:",
        "    def numDecodings(self, s):",
        "        prev2, prev1 = 1, 0 if s[0] == '0' else 1",
        "        for i in range(1, len(s)):",
        "            cur = 0 if s[i] == '0' else prev1",
        "            two = int(s[i - 1:i + 1])",
        "            if 10 <= two <= 26: cur += prev2",
        "            prev2, prev1 = prev1, cur",
        "        return prev1"
      ],
      "g2": [
        "class Solution:",
        "    def numDecodings(self, s):",
        "        prev2, prev1 = 1, 0 if s[0] == '0' else 1",
        "        for i in range(1, len(s)):",
        "            cur = 0 if s[i] == '0' else prev1",
        "            two = int(s[i - 1:i + 1])",
        "            if 10 <= two <= 26:",
        "                cur += prev2",
        "            prev2, prev1 = prev1, cur",
        "        return prev1"
      ]
    },
    "java": {
      "desktop": [
        "class Solution { public int numDecodings(String s) { int prev2 = 1, prev1 = s.charAt(0) == '0' ? 0 : 1; for (int i = 1; i < s.length(); i++) { int cur = s.charAt(i) == '0' ? 0 : prev1; int two = Integer.parseInt(s.substring(i - 1, i + 1)); if (two >= 10 && two <= 26) cur += prev2; prev2 = prev1; prev1 = cur; } return prev1; } }"
      ],
      "g2": [
        "class Solution {",
        "  public int numDecodings(String s) {",
        "    int prev2 = 1,",
        "      prev1 = s.charAt(0) == '0' ? 0 : 1;",
        "    for (int i = 1; i < s.length(); i++) {",
        "      int cur = s.charAt(i) == '0' ? 0 : prev1;",
        "      int two = Integer.parseInt(s.substring(i - 1, i + 1));",
        "      if (two >= 10 && two <= 26) {",
        "        cur += prev2;",
        "      }",
        "      prev2 = prev1;",
        "      prev1 = cur;",
        "    }",
        "    return prev1;",
        "  }",
        "}"
      ]
    },
    "cpp": {
      "desktop": [
        "class Solution { public: int numDecodings(string s) { int prev2 = 1, prev1 = s[0] == '0' ? 0 : 1; for (int i = 1; i < s.size(); i++) { int cur = s[i] == '0' ? 0 : prev1; int two = stoi(s.substr(i - 1, 2)); if (two >= 10 && two <= 26) cur += prev2; prev2 = prev1; prev1 = cur; } return prev1; } };"
      ],
      "g2": [
        "class Solution {",
        "public:",
        "  int numDecodings(string s) {",
        "    int prev2 = 1, prev1 = s[0] == '0' ? 0 : 1;",
        "    for (int i = 1; i < s.size(); i++) {",
        "      int cur = s[i] == '0' ? 0 : prev1;",
        "      int two = stoi(s.substr(i - 1, 2));",
        "      if (two >= 10 && two <= 26) {",
        "        cur += prev2;",
        "      }",
        "      prev2 = prev1;",
        "      prev1 = cur;",
        "    }",
        "    return prev1;",
        "  }",
        "};"
      ]
    }
  }
}
