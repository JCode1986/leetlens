{
  "id": 269,
  "slug": "alien-dictionary",
  "title": "Alien Dictionary",
  "difficulty": "Hard",
  "categories": [
    "Graphs",
    "Strings"
  ],
  "patterns": [
    "Topological Sort"
  ],
  "aliases": [
    "alien dictionary",
    "alien order",
    "foreign dictionary order"
  ],
  "keywords": [
    "graph",
    "topological",
    "string",
    "order",
    "cycle"
  ],
  "summary": "Solve Alien Dictionary using the standard optimized interview approach.",
  "hint": "Look for the Topological Sort structure that reduces repeated work.",
  "quickAnswer": {
    "pattern": "Topological Sort",
    "idea": [
      "Compare adjacent words to discover letter order.",
      "Build a directed graph of first differing letters.",
      "Topologically sort and detect invalid cycles."
    ],
    "complexity": {
      "time": "O(total characters)",
      "space": "O(1)"
    }
  },
  "approach": [
    "Identify the state or invariant needed for Alien Dictionary.",
    "Use Topological Sort to process the input efficiently.",
    "Update the result while preserving the invariant.",
    "Return the final value after all relevant input is processed."
  ],
  "pseudocode": [
    "create graph nodes for all letters",
    "",
    "for each adjacent word pair:",
    "    if invalid prefix order:",
    "        return empty string",
    "    find first differing letter",
    "    add ordering edge if found",
    "",
    "queue = letters with indegree zero",
    "order = empty string",
    "",
    "while queue is not empty:",
    "    append popped letter",
    "    reduce neighbor indegrees",
    "    push new zero indegrees",
    "",
    "return order if all letters used else empty"
  ],
  "complexity": {
    "time": "O(total characters)",
    "space": "O(1)"
  },
  "edgeCases": [
    "Empty or smallest valid input.",
    "Duplicate or repeated values.",
    "Result is not found.",
    "Large input near expected limits."
  ],
  "solutions": {
    "javascript": {
      "desktop": [
        "function alienOrder(words) { const graph = new Map(), indeg = new Map(); for (const w of words) for (const ch of w) { graph.set(ch, new Set()); indeg.set(ch, 0); } for (let i=1;i<words.length;i++){ const a=words[i-1], b=words[i]; if(a.startsWith(b)&&a.length>b.length) return ''; for(let j=0;j<Math.min(a.length,b.length);j++) if(a[j]!==b[j]){ if(!graph.get(a[j]).has(b[j])){ graph.get(a[j]).add(b[j]); indeg.set(b[j],indeg.get(b[j])+1); } break; } } const q=[...indeg].filter(x=>x[1]===0).map(x=>x[0]); let ans=''; for(let h=0;h<q.length;h++){ const ch=q[h]; ans+=ch; for(const n of graph.get(ch)) if(indeg.set(n,indeg.get(n)-1).get(n)===0) q.push(n); } return ans.length===indeg.size?ans:''; }"
      ],
      "g2": [
        "function alienOrder(words) {",
        "  const graph = new Map(), indeg = new Map();",
        "  for (const w of words) {",
        "    for (const ch of w) {",
        "    }",
        "    graph.set(ch, new Set());",
        "    indeg.set(ch, 0);",
        "  }",
        "  for (let i=1;i<words.length;i++) {",
        "    const a=words[i-1], b=words[i];",
        "    if(a.startsWith(b)&&a.length>b.length) {",
        "      return '';",
        "    }",
        "    for(let j=0;j<Math.min(a.length,b.length);j++) {",
        "      if(a[j]!==b[j]) {",
        "      }",
        "      if(!graph.get(a[j]).has(b[j])) {",
        "        graph.get(a[j]).add(b[j]);",
        "        indeg.set(b[j],indeg.get(b[j])+1);",
        "      }",
        "      break;",
        "    }",
        "  }",
        "  const q=[...indeg].filter(x=>x[1]===0).map(x=>x[0]);",
        "  let ans='';",
        "  for(let h=0;h<q.length;h++) {",
        "    const ch=q[h];",
        "    ans+=ch;",
        "    for(const n of graph.get(ch)) {",
        "      if(indeg.set(n,indeg.get(n)-1).get(n)===0) {",
        "        q.push(n);",
        "      }",
        "    }",
        "  }",
        "  return ans.length===indeg.size?ans:'';",
        "}"
      ]
    },
    "python": {
      "desktop": [
        "class Solution:",
        "    def alienOrder(self, words):",
        "        graph = {ch:set() for w in words for ch in w}; indeg = {ch:0 for ch in graph}",
        "        for a, b in zip(words, words[1:]):",
        "  if a.startswith(b) and len(a) > len(b): return ''",
        "  for x, y in zip(a, b):",
        "      if x != y:",
        "          if y not in graph[x]: graph[x].add(y); indeg[y] += 1",
        "          break",
        "        q = deque([ch for ch in indeg if indeg[ch] == 0]); ans = []",
        "        while q:",
        "  ch = q.popleft(); ans.append(ch)",
        "  for nxt in graph[ch]:",
        "      indeg[nxt] -= 1",
        "      if indeg[nxt] == 0: q.append(nxt)",
        "        return ''.join(ans) if len(ans) == len(indeg) else ''"
      ],
      "g2": [
        "class Solution:",
        "    def alienOrder(self, words):",
        "        graph = {ch:set() for w in words for ch in w}",
        "        indeg = {ch:0 for ch in graph}",
        "        for a, b in zip(words, words[1:]):",
        "            if a.startswith(b) and len(a) > len(b):",
        "                return ''",
        "            for x, y in zip(a, b):",
        "                if x != y:",
        "                    if y not in graph[x]:",
        "                        graph[x].add(y)",
        "                    indeg[y] += 1",
        "                    break",
        "        q = deque([ch for ch in indeg if indeg[ch] == 0])",
        "        ans = []",
        "        while q:",
        "            ch = q.popleft()",
        "            ans.append(ch)",
        "            for nxt in graph[ch]:",
        "                indeg[nxt] -= 1",
        "                if indeg[nxt] == 0:",
        "                    q.append(nxt)",
        "        return ''.join(ans) if len(ans) == len(indeg) else ''"
      ]
    },
    "java": {
      "desktop": [
        "class Solution { public String alienOrder(String[] words) { Map<Character,Set<Character>> graph=new HashMap<>(); Map<Character,Integer> indeg=new HashMap<>(); for(String w:words) for(char ch:w.toCharArray()){graph.putIfAbsent(ch,new HashSet<>()); indeg.putIfAbsent(ch,0);} for(int i=1;i<words.length;i++){String a=words[i-1],b=words[i]; if(a.startsWith(b)&&a.length()>b.length()) return \"\"; for(int j=0;j<Math.min(a.length(),b.length());j++) if(a.charAt(j)!=b.charAt(j)){ if(graph.get(a.charAt(j)).add(b.charAt(j))) indeg.put(b.charAt(j),indeg.get(b.charAt(j))+1); break; }} Queue<Character> q=new ArrayDeque<>(); for(char ch:indeg.keySet()) if(indeg.get(ch)==0) q.add(ch); StringBuilder ans=new StringBuilder(); while(!q.isEmpty()){char ch=q.remove(); ans.append(ch); for(char n:graph.get(ch)) if(indeg.merge(n,-1,Integer::sum)==0) q.add(n);} return ans.length()==indeg.size()?ans.toString():\"\"; } }"
      ],
      "g2": [
        "class Solution {",
        "  public String alienOrder(String[] words) {",
        "    Map<Character,Set<Character>> graph=new HashMap<>();",
        "    Map<Character,Integer> indeg=new HashMap<>();",
        "    for(String w:words) {",
        "      for(char ch:w.toCharArray()) {",
        "      }",
        "      graph.putIfAbsent(ch,new HashSet<>());",
        "      indeg.putIfAbsent(ch,0);",
        "    }",
        "    for(int i=1;i<words.length;i++) {",
        "      String a=words[i-1],b=words[i];",
        "      if(a.startsWith(b)&&a.length()>b.length()) {",
        "        return \"\";",
        "      }",
        "      for(int j=0;j<Math.min(a.length(),b.length());j++) {",
        "        if(a.charAt(j)!=b.charAt(j)) {",
        "        }",
        "        if(graph.get(a.charAt(j)).add(b.charAt(j))) {",
        "          indeg.put(b.charAt(j),indeg.get(b.charAt(j))+1);",
        "        }",
        "        break;",
        "      }",
        "    }",
        "    Queue<Character> q=new ArrayDeque<>();",
        "    for(char ch:indeg.keySet()) {",
        "      if(indeg.get(ch)==0) {",
        "        q.add(ch);",
        "      }",
        "    }",
        "    StringBuilder ans=new StringBuilder();",
        "    while(!q.isEmpty()) {",
        "      char ch=q.remove();",
        "      ans.append(ch);",
        "      for(char n:graph.get(ch)) {",
        "        if(indeg.merge(n,-1,Integer::sum)==0) {",
        "          q.add(n);",
        "        }",
        "      }",
        "    }",
        "    return ans.length()==indeg.size()?ans.toString():\"\";",
        "  }",
        "}"
      ]
    },
    "cpp": {
      "desktop": [
        "class Solution { public: string alienOrder(vector<string>& words) { unordered_map<char, unordered_set<char>> g; unordered_map<char,int> indeg; for(auto&w:words) for(char ch:w){g[ch]; indeg[ch];} for(int i=1;i<words.size();i++){auto&a=words[i-1]; auto&b=words[i]; if(a.size()>b.size()&&a.substr(0,b.size())==b) return \"\"; for(int j=0;j<min(a.size(),b.size());j++) if(a[j]!=b[j]){ if(!g[a[j]].count(b[j])){g[a[j]].insert(b[j]); indeg[b[j]]++;} break; }} queue<char> q; for(auto& [ch,d]:indeg) if(d==0) q.push(ch); string ans; while(!q.empty()){char ch=q.front();q.pop(); ans+=ch; for(char n:g[ch]) if(--indeg[n]==0) q.push(n);} return ans.size()==indeg.size()?ans:\"\"; } };"
      ],
      "g2": [
        "class Solution {",
        "public:",
        "  string alienOrder(vector<string>& words) {",
        "    unordered_map<char, unordered_set<char>> g;",
        "    unordered_map<char,int> indeg;",
        "    for(auto&w:words) {",
        "      for(char ch:w) {",
        "      }",
        "      g[ch];",
        "      indeg[ch];",
        "    }",
        "    for(int i=1;i<words.size();i++) {",
        "      auto&a=words[i-1];",
        "      auto&b=words[i];",
        "      if(a.size()>b.size()&&a.substr(0,b.size())==b) {",
        "        return \"\";",
        "      }",
        "      for(int j=0;j<min(a.size(),b.size());j++) {",
        "        if(a[j]!=b[j]) {",
        "        }",
        "        if(!g[a[j]].count(b[j])) {",
        "          g[a[j]].insert(b[j]);",
        "          indeg[b[j]]++;",
        "        }",
        "        break;",
        "      }",
        "    }",
        "    queue<char> q;",
        "    for(auto& [ch,d]:indeg) {",
        "      if(d==0) {",
        "        q.push(ch);",
        "      }",
        "    }",
        "    string ans;",
        "    while(!q.empty()) {",
        "      char ch=q.front();",
        "      q.pop();",
        "      ans+=ch;",
        "      for(char n:g[ch]) {",
        "        if(--indeg[n]==0) {",
        "          q.push(n);",
        "        }",
        "      }",
        "    }",
        "    return ans.size()==indeg.size()?ans:\"\";",
        "  }",
        "};"
      ]
    }
  }
}
