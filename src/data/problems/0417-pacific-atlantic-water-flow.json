{
  "id": 417,
  "slug": "pacific-atlantic-water-flow",
  "title": "Pacific Atlantic Water Flow",
  "difficulty": "Medium",
  "categories": [
    "Matrix",
    "Graphs",
    "DFS",
    "BFS"
  ],
  "patterns": [
    "Graph Traversal",
    "DFS",
    "BFS"
  ],
  "aliases": [
    "pacific atlantic",
    "water flow",
    "pacific atlantic water",
    "ocean water flow"
  ],
  "keywords": [
    "matrix",
    "graph",
    "dfs",
    "bfs",
    "ocean",
    "water"
  ],
  "summary": "Solve Pacific Atlantic Water Flow using the standard optimized interview approach.",
  "hint": "Look for the Graph Traversal structure that reduces repeated work.",
  "quickAnswer": {
    "pattern": "Reverse Graph DFS",
    "idea": [
      "Water can flow from high to low.",
      "Reverse the search from each ocean inward.",
      "Cells reached by both oceans are answers."
    ],
    "complexity": {
      "time": "O(m * n)",
      "space": "O(m * n)"
    }
  },
  "approach": [
    "Identify the state or invariant needed for Pacific Atlantic Water Flow.",
    "Use Graph Traversal and DFS and BFS to process the input efficiently.",
    "Update the result while preserving the invariant.",
    "Return the final value after all relevant input is processed."
  ],
  "pseudocode": [
    "pacific = empty set",
    "atlantic = empty set",
    "",
    "dfs(cell, visited, previousHeight):",
    "    if cell invalid or already visited:",
    "        return",
    "    if cell height < previousHeight:",
    "        return",
    "",
    "    mark visited",
    "    visit four neighbors",
    "",
    "run dfs from pacific borders",
    "run dfs from atlantic borders",
    "return cells in both sets"
  ],
  "complexity": {
    "time": "O(m * n)",
    "space": "O(m * n)"
  },
  "edgeCases": [
    "Empty or smallest valid input.",
    "Duplicate or repeated values.",
    "Result is not found.",
    "Large input near expected limits."
  ],
  "solutions": {
    "javascript": {
      "desktop": [
        "function pacificAtlantic(heights) { const rows=heights.length, cols=heights[0].length; const make=()=>Array.from({length:rows},()=>Array(cols).fill(false)); const pac=make(), atl=make(); function dfs(r,c,seen,prev){ if(r<0||c<0||r===rows||c===cols||seen[r][c]||heights[r][c]<prev) return; seen[r][c]=true; dfs(r+1,c,seen,heights[r][c]); dfs(r-1,c,seen,heights[r][c]); dfs(r,c+1,seen,heights[r][c]); dfs(r,c-1,seen,heights[r][c]); } for(let r=0;r<rows;r++){ dfs(r,0,pac,-1); dfs(r,cols-1,atl,-1); } for(let c=0;c<cols;c++){ dfs(0,c,pac,-1); dfs(rows-1,c,atl,-1); } const ans=[]; for(let r=0;r<rows;r++) for(let c=0;c<cols;c++) if(pac[r][c]&&atl[r][c]) ans.push([r,c]); return ans; }"
      ],
      "g2": [
        "function pacificAtlantic(heights) {",
        "  const rows=heights.length,",
        "    cols=heights[0].length;",
        "  const make=()=>Array.from( {",
        "    length:rows",
        "  }",
        "  ,()=>Array(cols).fill(false));",
        "  const pac=make(), atl=make();",
        "  function dfs(r,c,seen,prev) {",
        "    if(r<0||c<0||r===rows||c===cols||seen[r][c]||heights[r][c]<prev) {",
        "      return;",
        "    }",
        "    seen[r][c]=true;",
        "    dfs(r+1,c,seen,heights[r][c]);",
        "    dfs(r-1,c,seen,heights[r][c]);",
        "    dfs(r,c+1,seen,heights[r][c]);",
        "    dfs(r,c-1,seen,heights[r][c]);",
        "  }",
        "  for(let r=0;r<rows;r++) {",
        "    dfs(r,0,pac,-1);",
        "    dfs(r,cols-1,atl,-1);",
        "  }",
        "  for(let c=0;c<cols;c++) {",
        "    dfs(0,c,pac,-1);",
        "    dfs(rows-1,c,atl,-1);",
        "  }",
        "  const ans=[];",
        "  for(let r=0;r<rows;r++) {",
        "    for(let c=0;c<cols;c++) {",
        "      if(pac[r][c]&&atl[r][c]) {",
        "        ans.push([r,c]);",
        "      }",
        "    }",
        "  }",
        "  return ans;",
        "}"
      ]
    },
    "python": {
      "desktop": [
        "class Solution:",
        "    def pacificAtlantic(self, heights):",
        "        rows, cols = len(heights), len(heights[0])",
        "        pac, atl = set(), set()",
        "        def dfs(r, c, seen, prev):",
        "  if r < 0 or c < 0 or r == rows or c == cols or (r,c) in seen or heights[r][c] < prev: return",
        "  seen.add((r,c))",
        "  for dr, dc in ((1,0),(-1,0),(0,1),(0,-1)): dfs(r+dr, c+dc, seen, heights[r][c])",
        "        for r in range(rows): dfs(r,0,pac,-1); dfs(r,cols-1,atl,-1)",
        "        for c in range(cols): dfs(0,c,pac,-1); dfs(rows-1,c,atl,-1)",
        "        return list(pac & atl)"
      ],
      "g2": [
        "class Solution:",
        "    def pacificAtlantic(self, heights):",
        "        rows, cols = len(heights), len(heights[0])",
        "        pac, atl = set(), set()",
        "        def dfs(r, c, seen, prev):",
        "            if r < 0 or c < 0 or r == rows or c == cols or (r,c) in seen or heights[r][c] < prev:",
        "                return",
        "            seen.add((r,c))",
        "            for dr, dc in ((1,0),(-1,0),(0,1),(0,-1)):",
        "                dfs(r+dr, c+dc, seen, heights[r][c])",
        "        for r in range(rows):",
        "            dfs(r,0,pac,-1)",
        "        dfs(r,cols-1,atl,-1)",
        "        for c in range(cols):",
        "            dfs(0,c,pac,-1)",
        "        dfs(rows-1,c,atl,-1)",
        "        return list(pac & atl)"
      ]
    },
    "java": {
      "desktop": [
        "class Solution { public List<List<Integer>> pacificAtlantic(int[][] h) { int m=h.length,n=h[0].length; boolean[][] p=new boolean[m][n], a=new boolean[m][n]; for(int r=0;r<m;r++){dfs(h,r,0,p,-1);dfs(h,r,n-1,a,-1);} for(int c=0;c<n;c++){dfs(h,0,c,p,-1);dfs(h,m-1,c,a,-1);} List<List<Integer>> ans=new ArrayList<>(); for(int r=0;r<m;r++) for(int c=0;c<n;c++) if(p[r][c]&&a[r][c]) ans.add(Arrays.asList(r,c)); return ans; } void dfs(int[][] h,int r,int c,boolean[][] seen,int prev){ if(r<0||c<0||r==h.length||c==h[0].length||seen[r][c]||h[r][c]<prev) return; seen[r][c]=true; dfs(h,r+1,c,seen,h[r][c]); dfs(h,r-1,c,seen,h[r][c]); dfs(h,r,c+1,seen,h[r][c]); dfs(h,r,c-1,seen,h[r][c]); } }"
      ],
      "g2": [
        "class Solution {",
        "  public List<List<Integer>> pacificAtlantic(int[][] h) {",
        "    int m=h.length,n=h[0].length;",
        "    boolean[][] p=new boolean[m][n],",
        "      a=new boolean[m][n];",
        "    for(int r=0;r<m;r++) {",
        "      dfs(h,r,0,p,-1);",
        "      dfs(h,r,n-1,a,-1);",
        "    }",
        "    for(int c=0;c<n;c++) {",
        "      dfs(h,0,c,p,-1);",
        "      dfs(h,m-1,c,a,-1);",
        "    }",
        "    List<List<Integer>> ans=new ArrayList<>();",
        "    for(int r=0;r<m;r++) {",
        "      for(int c=0;c<n;c++) {",
        "        if(p[r][c]&&a[r][c]) {",
        "          ans.add(Arrays.asList(r,c));",
        "        }",
        "      }",
        "    }",
        "    return ans;",
        "  }",
        "  void dfs(int[][] h,int r,int c,boolean[][] seen,int prev) {",
        "    if(r<0||c<0||r==h.length||c==h[0].length||seen[r][c]||h[r][c]<prev) {",
        "      return;",
        "    }",
        "    seen[r][c]=true;",
        "    dfs(h,r+1,c,seen,h[r][c]);",
        "    dfs(h,r-1,c,seen,h[r][c]);",
        "    dfs(h,r,c+1,seen,h[r][c]);",
        "    dfs(h,r,c-1,seen,h[r][c]);",
        "  }",
        "}"
      ]
    },
    "cpp": {
      "desktop": [
        "class Solution { void dfs(vector<vector<int>>& h,int r,int c,vector<vector<int>>& seen,int prev){ if(r<0||c<0||r==h.size()||c==h[0].size()||seen[r][c]||h[r][c]<prev) return; seen[r][c]=1; dfs(h,r+1,c,seen,h[r][c]); dfs(h,r-1,c,seen,h[r][c]); dfs(h,r,c+1,seen,h[r][c]); dfs(h,r,c-1,seen,h[r][c]); } public: vector<vector<int>> pacificAtlantic(vector<vector<int>>& h) { int m=h.size(),n=h[0].size(); vector<vector<int>> p(m,vector<int>(n)),a(p); for(int r=0;r<m;r++){dfs(h,r,0,p,-1);dfs(h,r,n-1,a,-1);} for(int c=0;c<n;c++){dfs(h,0,c,p,-1);dfs(h,m-1,c,a,-1);} vector<vector<int>> ans; for(int r=0;r<m;r++) for(int c=0;c<n;c++) if(p[r][c]&&a[r][c]) ans.push_back({r,c}); return ans; } };"
      ],
      "g2": [
        "class Solution {",
        "  void dfs(vector<vector<int>>& h,int r,int c,vector<vector<int>>& seen,int prev) {",
        "    if(r<0||c<0||r==h.size()||c==h[0].size()||seen[r][c]||h[r][c]<prev) {",
        "      return;",
        "    }",
        "    seen[r][c]=1;",
        "    dfs(h,r+1,c,seen,h[r][c]);",
        "    dfs(h,r-1,c,seen,h[r][c]);",
        "    dfs(h,r,c+1,seen,h[r][c]);",
        "    dfs(h,r,c-1,seen,h[r][c]);",
        "  }",
        "public:",
        "  vector<vector<int>> pacificAtlantic(vector<vector<int>>& h) {",
        "    int m=h.size(),n=h[0].size();",
        "    vector<vector<int>> p(m,vector<int>(n)),a(p);",
        "    for(int r=0;r<m;r++) {",
        "      dfs(h,r,0,p,-1);",
        "      dfs(h,r,n-1,a,-1);",
        "    }",
        "    for(int c=0;c<n;c++) {",
        "      dfs(h,0,c,p,-1);",
        "      dfs(h,m-1,c,a,-1);",
        "    }",
        "    vector<vector<int>> ans;",
        "    for(int r=0;r<m;r++) {",
        "      for(int c=0;c<n;c++) {",
        "        if(p[r][c]&&a[r][c]) {",
        "          ans.push_back( {",
        "          }",
        "        }",
        "      }",
        "      r,c",
        "    }",
        "    );",
        "    return ans;",
        "  }",
        "};"
      ]
    }
  }
}
