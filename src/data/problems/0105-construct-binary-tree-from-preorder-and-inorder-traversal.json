{
  "id": 105,
  "slug": "construct-binary-tree-from-preorder-and-inorder-traversal",
  "title": "Construct Binary Tree from Preorder and Inorder Traversal",
  "difficulty": "Medium",
  "categories": [
    "Trees",
    "Hash Maps"
  ],
  "patterns": [
    "Tree Recursion"
  ],
  "aliases": [
    "construct binary tree",
    "build tree preorder inorder",
    "preorder inorder tree"
  ],
  "keywords": [
    "tree",
    "preorder",
    "inorder",
    "recursion",
    "hash map"
  ],
  "summary": "Solve Construct Binary Tree from Preorder and Inorder Traversal using the standard optimized interview approach.",
  "hint": "Look for the Tree Recursion structure that reduces repeated work.",
  "quickAnswer": {
    "pattern": "Tree Recursion",
    "idea": [
      "Preorder gives the root first.",
      "Inorder splits left and right subtrees.",
      "Use indices to build each subtree once."
    ],
    "complexity": {
      "time": "O(n)",
      "space": "O(n)"
    }
  },
  "approach": [
    "Identify the state or invariant needed for Construct Binary Tree from Preorder and Inorder Traversal.",
    "Use Tree Recursion to process the input efficiently.",
    "Update the result while preserving the invariant.",
    "Return the final value after all relevant input is processed."
  ],
  "pseudocode": [
    "map inorder value to index",
    "preIndex = 0",
    "",
    "build(leftBound, rightBound):",
    "    if bounds are empty:",
    "        return null",
    "",
    "    rootValue = preorder[preIndex]",
    "    advance preIndex",
    "    root = new node",
    "    split = inorder index",
    "    root.left = build(leftBound, split - 1)",
    "    root.right = build(split + 1, rightBound)",
    "    return root",
    "",
    "return build(0, n - 1)"
  ],
  "complexity": {
    "time": "O(n)",
    "space": "O(n)"
  },
  "edgeCases": [
    "Empty or smallest valid input.",
    "Duplicate or repeated values.",
    "Result is not found.",
    "Large input near expected limits."
  ],
  "solutions": {
    "javascript": {
      "desktop": [
        "function buildTree(preorder, inorder) { const pos = new Map(); inorder.forEach((v,i)=>pos.set(v,i)); let pre = 0; function build(l,r){ if(l>r) return null; const root = new TreeNode(preorder[pre++]); const m = pos.get(root.val); root.left = build(l,m-1); root.right = build(m+1,r); return root; } return build(0,inorder.length-1); }"
      ],
      "g2": [
        "function buildTree(preorder, inorder) {",
        "  const pos = new Map();",
        "  inorder.forEach((v,i)=>pos.set(v,i));",
        "  let pre = 0;",
        "  function build(l,r) {",
        "    if(l>r) {",
        "      return null;",
        "    }",
        "    const root = new TreeNode(preorder[pre++]);",
        "    const m = pos.get(root.val);",
        "    root.left = build(l,m-1);",
        "    root.right = build(m+1,r);",
        "    return root;",
        "  }",
        "  return build(0,inorder.length-1);",
        "}"
      ]
    },
    "python": {
      "desktop": [
        "class Solution:",
        "    def buildTree(self, preorder, inorder):",
        "        pos = {v:i for i,v in enumerate(inorder)}; pre = 0",
        "        def build(l, r):",
        "  nonlocal pre",
        "  if l > r: return None",
        "  root = TreeNode(preorder[pre]); pre += 1",
        "  m = pos[root.val]",
        "  root.left = build(l, m - 1); root.right = build(m + 1, r)",
        "  return root",
        "        return build(0, len(inorder) - 1)"
      ],
      "g2": [
        "class Solution:",
        "    def buildTree(self, preorder, inorder):",
        "        pos = {v:i for i,v in enumerate(inorder)}",
        "        pre = 0",
        "        def build(l, r):",
        "            nonlocal pre",
        "            if l > r:",
        "                return None",
        "            root = TreeNode(preorder[pre])",
        "            pre += 1",
        "            m = pos[root.val]",
        "            root.left = build(l, m - 1)",
        "            root.right = build(m + 1, r)",
        "            return root",
        "        return build(0, len(inorder) - 1)"
      ]
    },
    "java": {
      "desktop": [
        "class Solution { int pre = 0; Map<Integer,Integer> pos = new HashMap<>(); public TreeNode buildTree(int[] preorder, int[] inorder) { for (int i = 0; i < inorder.length; i++) pos.put(inorder[i], i); return build(preorder, 0, inorder.length - 1); } private TreeNode build(int[] preorder, int l, int r) { if (l > r) return null; TreeNode root = new TreeNode(preorder[pre++]); int m = pos.get(root.val); root.left = build(preorder, l, m - 1); root.right = build(preorder, m + 1, r); return root; } }"
      ],
      "g2": [
        "class Solution {",
        "  int pre = 0;",
        "  Map<Integer,Integer> pos = new HashMap<>();",
        "  public TreeNode buildTree(int[] preorder, int[] inorder) {",
        "    for (int i = 0; i < inorder.length; i++) {",
        "      pos.put(inorder[i], i);",
        "    }",
        "    return build(preorder, 0,",
        "      inorder.length - 1);",
        "  }",
        "  private TreeNode build(int[] preorder, int l, int r) {",
        "    if (l > r) {",
        "      return null;",
        "    }",
        "    TreeNode root = new TreeNode(preorder[pre++]);",
        "    int m = pos.get(root.val);",
        "    root.left = build(preorder, l, m - 1);",
        "    root.right = build(preorder, m + 1, r);",
        "    return root;",
        "  }",
        "}"
      ]
    },
    "cpp": {
      "desktop": [
        "class Solution { int pre = 0; unordered_map<int,int> pos; TreeNode* build(vector<int>& preorder, int l, int r) { if (l > r) return nullptr; auto root = new TreeNode(preorder[pre++]); int m = pos[root->val]; root->left = build(preorder,l,m-1); root->right = build(preorder,m+1,r); return root; } public: TreeNode* buildTree(vector<int>& preorder, vector<int>& inorder) { for (int i=0;i<inorder.size();i++) pos[inorder[i]]=i; return build(preorder,0,inorder.size()-1); } };"
      ],
      "g2": [
        "class Solution {",
        "  int pre = 0;",
        "  unordered_map<int,int> pos;",
        "  TreeNode* build(vector<int>& preorder, int l, int r) {",
        "    if (l > r) {",
        "      return nullptr;",
        "    }",
        "    auto root = new TreeNode(preorder[pre++]);",
        "    int m = pos[root->val];",
        "    root->left = build(preorder,l,m-1);",
        "    root->right = build(preorder,m+1,r);",
        "    return root;",
        "  }",
        "public:",
        "  TreeNode* buildTree(vector<int>& preorder, vector<int>& inorder) {",
        "    for (int i=0;i<inorder.size();i++) {",
        "      pos[inorder[i]]=i;",
        "    }",
        "    return build(preorder,0,inorder.size()-1);",
        "  }",
        "};"
      ]
    }
  }
}
