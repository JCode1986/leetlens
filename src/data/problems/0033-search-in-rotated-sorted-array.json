{
  "id": 33,
  "slug": "search-in-rotated-sorted-array",
  "title": "Search in Rotated Sorted Array",
  "difficulty": "Medium",
  "categories": [
    "Arrays",
    "Binary Search"
  ],
  "patterns": [
    "Binary Search"
  ],
  "aliases": [
    "search rotated array",
    "rotated sorted array search",
    "find target rotated array"
  ],
  "keywords": [
    "array",
    "binary search",
    "rotated",
    "target"
  ],
  "summary": "Solve Search in Rotated Sorted Array using the standard optimized interview approach.",
  "hint": "Look for the Binary Search structure that reduces repeated work.",
  "quickAnswer": {
    "pattern": "Modified Binary Search",
    "idea": [
      "One half is always sorted.",
      "Use target range to choose the useful half.",
      "Discard the other half each step."
    ],
    "complexity": {
      "time": "O(log n)",
      "space": "O(1)"
    }
  },
  "approach": [
    "Identify the state or invariant needed for Search in Rotated Sorted Array.",
    "Use Binary Search to process the input efficiently.",
    "Update the result while preserving the invariant.",
    "Return the final value after all relevant input is processed."
  ],
  "pseudocode": [
    "left = 0",
    "right = last index",
    "",
    "while left <= right:",
    "    mid = middle index",
    "    if nums[mid] is target:",
    "        return mid",
    "",
    "    if left half is sorted:",
    "        if target is in left half:",
    "            move right before mid",
    "        else:",
    "            move left after mid",
    "    else:",
    "        if target is in right half:",
    "            move left after mid",
    "        else:",
    "            move right before mid",
    "",
    "return -1"
  ],
  "complexity": {
    "time": "O(log n)",
    "space": "O(1)"
  },
  "edgeCases": [
    "Empty or smallest valid input.",
    "Duplicate or repeated values.",
    "Result is not found.",
    "Large input near expected limits."
  ],
  "solutions": {
    "javascript": {
      "desktop": [
        "function search(nums, target) {",
        "  let left = 0, right = nums.length - 1;",
        "  while (left <= right) {",
        "    const mid = Math.floor((left + right) / 2);",
        "    if (nums[mid] === target) return mid;",
        "    if (nums[left] <= nums[mid]) {",
        "      if (nums[left] <= target && target < nums[mid]) right = mid - 1; else left = mid + 1;",
        "    } else {",
        "      if (nums[mid] < target && target <= nums[right]) left = mid + 1; else right = mid - 1;",
        "    }",
        "  }",
        "  return -1;",
        "}"
      ],
      "g2": [
        "function search(nums, target) {",
        "  let left = 0, right = nums.length - 1;",
        "  while (left <= right) {",
        "    const mid = Math.floor((left + right) / 2);",
        "    if (nums[mid] === target) {",
        "      return mid;",
        "    }",
        "    if (nums[left] <= nums[mid]) {",
        "      if (nums[left] <= target && target < nums[mid]) {",
        "        right = mid - 1;",
        "      }",
        "      else {",
        "        left = mid + 1;",
        "      }",
        "    }",
        "    else {",
        "      if (nums[mid] < target && target <= nums[right]) {",
        "        left = mid + 1;",
        "      }",
        "      else {",
        "        right = mid - 1;",
        "      }",
        "    }",
        "  }",
        "  return -1;",
        "}"
      ]
    },
    "python": {
      "desktop": [
        "class Solution:",
        "    def search(self, nums, target):",
        "        left, right = 0, len(nums) - 1",
        "        while left <= right:",
        "            mid = (left + right) // 2",
        "            if nums[mid] == target:",
        "                return mid",
        "            if nums[left] <= nums[mid]:",
        "                if nums[left] <= target < nums[mid]: right = mid - 1",
        "                else: left = mid + 1",
        "            else:",
        "                if nums[mid] < target <= nums[right]: left = mid + 1",
        "                else: right = mid - 1",
        "        return -1"
      ],
      "g2": [
        "class Solution:",
        "    def search(self, nums, target):",
        "        left, right = 0, len(nums) - 1",
        "        while left <= right:",
        "            mid = (left + right) // 2",
        "            if nums[mid] == target:",
        "                return mid",
        "            if nums[left] <= nums[mid]:",
        "                if nums[left] <= target < nums[mid]:",
        "                    right = mid - 1",
        "                else:",
        "                    left = mid + 1",
        "            else:",
        "                if nums[mid] < target <= nums[right]:",
        "                    left = mid + 1",
        "                else:",
        "                    right = mid - 1",
        "        return -1"
      ]
    },
    "java": {
      "desktop": [
        "class Solution {",
        "    public int search(int[] nums, int target) {",
        "        int left = 0, right = nums.length - 1;",
        "        while (left <= right) {",
        "            int mid = left + (right - left) / 2;",
        "            if (nums[mid] == target) return mid;",
        "            if (nums[left] <= nums[mid]) {",
        "                if (nums[left] <= target && target < nums[mid]) right = mid - 1; else left = mid + 1;",
        "            } else {",
        "                if (nums[mid] < target && target <= nums[right]) left = mid + 1; else right = mid - 1;",
        "            }",
        "        }",
        "        return -1;",
        "    }",
        "}"
      ],
      "g2": [
        "class Solution {",
        "  public int search(int[] nums, int target) {",
        "    int left = 0, right = nums.length - 1;",
        "    while (left <= right) {",
        "      int mid = left + (right - left) / 2;",
        "      if (nums[mid] == target) {",
        "        return mid;",
        "      }",
        "      if (nums[left] <= nums[mid]) {",
        "        if (nums[left] <= target && target < nums[mid]) {",
        "          right = mid - 1;",
        "        }",
        "        else {",
        "          left = mid + 1;",
        "        }",
        "      }",
        "      else {",
        "        if (nums[mid] < target && target <= nums[right]) {",
        "          left = mid + 1;",
        "        }",
        "        else {",
        "          right = mid - 1;",
        "        }",
        "      }",
        "    }",
        "    return -1;",
        "  }",
        "}"
      ]
    },
    "cpp": {
      "desktop": [
        "class Solution {",
        "public:",
        "    int search(vector<int>& nums, int target) {",
        "        int left = 0, right = nums.size() - 1;",
        "        while (left <= right) {",
        "            int mid = left + (right - left) / 2;",
        "            if (nums[mid] == target) return mid;",
        "            if (nums[left] <= nums[mid]) {",
        "                if (nums[left] <= target && target < nums[mid]) right = mid - 1; else left = mid + 1;",
        "            } else {",
        "                if (nums[mid] < target && target <= nums[right]) left = mid + 1; else right = mid - 1;",
        "            }",
        "        }",
        "        return -1;",
        "    }",
        "};"
      ],
      "g2": [
        "class Solution {",
        "public:",
        "  int search(vector<int>& nums, int target) {",
        "    int left = 0, right = nums.size() - 1;",
        "    while (left <= right) {",
        "      int mid = left + (right - left) / 2;",
        "      if (nums[mid] == target) {",
        "        return mid;",
        "      }",
        "      if (nums[left] <= nums[mid]) {",
        "        if (nums[left] <= target && target < nums[mid]) {",
        "          right = mid - 1;",
        "        }",
        "        else {",
        "          left = mid + 1;",
        "        }",
        "      }",
        "      else {",
        "        if (nums[mid] < target && target <= nums[right]) {",
        "          left = mid + 1;",
        "        }",
        "        else {",
        "          right = mid - 1;",
        "        }",
        "      }",
        "    }",
        "    return -1;",
        "  }",
        "};"
      ]
    }
  }
}
